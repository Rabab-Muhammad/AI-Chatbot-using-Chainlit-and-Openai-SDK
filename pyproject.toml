[project]
name = "ai-chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chainlit==2.6.0",
    "openai-agents>=0.2.8",
    "python-decouple>=3.8",
    "python-dotenv>=1.1.1",
]
